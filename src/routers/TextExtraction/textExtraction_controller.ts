import { FastifyReply, FastifyRequest } from "fastify";

export const textExtractionController = {
	createList: async (req: FastifyRequest<{Body: any, Params: any, Reply: any}>, reply: FastifyReply) => {
		
	},
}